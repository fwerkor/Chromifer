schema_version = 1

# Count the complete Chromium-authored production implementation selected by
# the GN flag. The DataDecoderService receiver handoff is included on both
# sides. The candidate does not depend on the pre-existing C++ XML DOM path;
# GN dependency checks in the focused evidence explicitly enforce that.
[baseline]
files = [
  "services/data_decoder/data_decoder_service.cc",
  "services/data_decoder/xml_parser.cc",
  "services/data_decoder/xml_parser.h",
]
cross_language_forwarding_methods = []

[candidate]
files = [
  "services/data_decoder/data_decoder_service.cc",
  "services/data_decoder/xml/xml_parser_mojo.rs",
]
cross_language_forwarding_methods = [
  "data_decoder::BindXmlParser",
]

[contract_review]
new_public_api_count = 0
new_mojom_method_count = 0
